const print = () => {
  console.log()
};

const whatTakaThatGive = (something) => {
  return something;
};

const takeOnlyMass = (mass = []) => {
  return mass;
};

const takeOnlyFunc = (callBack)=>{
  return callBack;
};

const takeOnlyNumber = (number= 1)=>{
  return number;
};

const takeOnlyStr = (str='')=> {
  return str;
};



takeOnlyMass([1,2,3]).map((asd)=>{
  console.log(asd);
})

